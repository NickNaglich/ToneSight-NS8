# Server Scaffold (v0.2.5 Phase 1)

This directory is reserved for the minimal static artifact API.

Phase 1 contract:
- serve existing artifacts verbatim from `runs/`
- no scoring/compare/gate recomputation
- deterministic error payloads for missing artifacts
