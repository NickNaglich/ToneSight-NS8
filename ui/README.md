# UI Scaffold (v0.2.5 Phase 1)

This directory is reserved for the read-only artifact UI.

Phase 1 contract:
- consume deterministic artifacts only
- do not compute new scores, bins, or gate decisions
- render existing run/compare/gate outputs
